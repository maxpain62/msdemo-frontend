apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "msdemo-frontend-chart.fullname" . }}-test-connection"
  labels:
    {{- include "msdemo-frontend-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "msdemo-frontend-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
