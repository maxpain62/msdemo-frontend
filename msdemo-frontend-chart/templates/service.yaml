apiVersion: v1
kind: Service
metadata:
  name: {{ include "msdemo-frontend-chart.fullname" . }}
  labels:
    {{- include "msdemo-frontend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msdemo-frontend-chart.selectorLabels" . | nindent 4 }}
